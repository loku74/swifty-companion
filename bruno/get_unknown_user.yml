info:
  name: Get unknown user
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/v2/users/{{unknownLogin}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // users.ts turns this 404 into "No student found with the login ...".
        test("returns 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
